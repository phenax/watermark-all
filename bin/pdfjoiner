#!/bin/bash
# Join all pages in a pdf into one image

PDF_FILE="$1"
OUTPUT_IMAGE="$2"
PDF_FILE_SIZE=$(identify -format %w $PDF_FILE)

convert \
	-fill white \
	-density 300 \
	$PDF_FILE \
	-quality 100 \
	-sharpen 0x1.0 \
	-background white \
	-append $OUTPUT_IMAGE;
